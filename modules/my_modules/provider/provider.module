<?php

function provider_page_attachments(array &$attachments) {
    if(\Drupal::request()->getRequestUri()=='/provider') {
        $attachments['#attached']['library'][] = 'provider/provider_lib';
    }
}